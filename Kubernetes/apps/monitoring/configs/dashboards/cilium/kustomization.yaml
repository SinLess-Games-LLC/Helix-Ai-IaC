apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
configMapGenerator:
  - name: cilium-monitoring-dashboards
    files:
      - cilium-dashboard.json
      - hubble-dashboard.json
    options:
      disableNameSuffixHash: true
      labels:
        grafana_dashboard: "1"