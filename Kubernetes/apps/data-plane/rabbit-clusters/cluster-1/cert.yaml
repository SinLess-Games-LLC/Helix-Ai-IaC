apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rabbitmq-tls
  namespace: data-plane
spec:
  secretName: rabbitmq-tls-secret
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-staging
  dnsNames:
    - cluster-1.rabbitmq.${SECRET_DOMAIN}