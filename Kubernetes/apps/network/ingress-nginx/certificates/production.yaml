---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "${SECRET_DOMAIN1/./-}-production"
spec:
  secretName: "${SECRET_DOMAIN1/./-}-production-tls"
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "${SECRET_DOMAIN1}"
  dnsNames:
    - "${SECRET_DOMAIN1}"
    - "*.${SECRET_DOMAIN1}"
